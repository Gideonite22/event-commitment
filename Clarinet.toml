[project]
name = "event-commitment"
authors = ["Your Name <your.email@example.com>"]
description = "A decentralized system for tracking and verifying event commitments"
telemetry = false
requirements = []
[contracts.event-commitment]
path = "contracts/event-commitment.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
